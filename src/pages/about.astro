---
import BaseLayout from '@layouts/BaseLayout.astro';
import AboutSection from '@components/About.astro';
import { sanityClient } from 'sanity:client';

const contentAbout = await sanityClient.fetch(`*[_type == "aboutPage" && !(_originalId in path("drafts.**"))][0]`);
---

<BaseLayout title="Sobre Nós - Next Generation Academy">
    <AboutSection content={{
        about: contentAbout,
    }} />
</BaseLayout>


